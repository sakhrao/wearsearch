export const demoProviderInfo = {
  id: "demo",
  sourceName: "WearSearch Demo Store",
  description:
    "79-product curated catalog (plus StyleHub Affiliate Feed) persisted by prisma/seed.ts. No remote fetch; the app stays fully usable from this data when external providers fail.",
} as const;
